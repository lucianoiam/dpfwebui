{
  "scripts": {
    "asbuild:untouched": "asc assembly/glue.ts --target debug",
    "asbuild:optimized": "asc assembly/glue.ts --target release",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized",
    "test": "node tests"
  },
  "dependencies": {
    "@assemblyscript/loader": "^0.19.7"
  },
  "devDependencies": {
    "assemblyscript": "^0.19.7"
  }
}